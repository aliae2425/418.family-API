{% extends 'base.html.twig' %}

{% block title %}Profil : {{ user.fullName }} {% endblock %}

{% block body %}
<div class="flex">
    <!-- Sidebar -->
    <div class="w-1/4 p-4">
        <div class="bg-white dark:bg-gray-800 p-6 rounded-lg shadow-md">
            <h2 class="text-xl font-bold mb-4 text-gray-900 dark:text-gray-100">Menu</h2>
            <ul>
                <li class="mb-2"><a href="{{ path('user_profile') }}" class="text-gray-700 dark:text-gray-300 hover:text-gray-400 dark:hover:text-gray-500">Dashboard</a></li>
                <li class="mb-2"><a href="{{ path('User_business_admin') }}" class="text-gray-700 dark:text-gray-300 hover:text-gray-400 dark:hover:text-gray-500">Utilisateurs</a></li>
                <li class="mb-2"><a href="#" class="text-gray-700 dark:text-gray-300 hover:text-gray-400 dark:hover:text-gray-500">Détails</a></li>
            </ul>
        </div>
    </div>

    <div class="w-3/4 p-4">
    {% block content %}
    
    {% endblock %}
    </div>
</div>
{% endblock %}
